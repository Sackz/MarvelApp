<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.papena.marvelsapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:name=".MarvelsApp"
        android:icon="@mipmap/marvel_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/marvel_icon_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MarvelsApp">
        <activity
            android:name=".features.home.ui.HomeActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".features.detail.ui.DetailActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".features.filter.ui.FilterActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>